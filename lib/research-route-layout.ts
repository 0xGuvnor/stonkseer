/** Viewport-fill centered column (aligned with home search); blocking / loading views on ticker research. */
export const RESEARCH_ROUTE_CENTER_SHELL =
  "mx-auto flex min-h-full w-full max-w-5xl flex-col items-center justify-center gap-8 px-6 pb-6 md:pb-12"

/** Shared mobile centering shell for hero and empty-state pages. */
export const MOBILE_CENTER_PAGE_SHELL =
  "mx-auto flex min-h-full flex-col items-center justify-center px-5 text-center sm:px-6"
