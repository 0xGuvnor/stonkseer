## Learned User Preferences

- Do **not** add React memoization hooks (`useCallback`, `useMemo`, `React.memo`) unless there is a **concrete, demonstrated benefit**—e.g. a memoized child that re-renders expensively on callback identity, a callback in a hook dependency array where stability matters, or a large virtualized list. Default to plain functions, inline handlers, and direct values; do not reach for these hooks out of habit or “best practice” boilerplate.
- For UI, **default to shadcn/ui**: look under `components/ui/` for the primitive; if it is not installed yet, add it with `bunx shadcn@latest add <component>` (then compose from it) instead of hand-rolling tables, dialogs, forms, etc., unless the user explicitly asks for a custom implementation. Prefer shadcn **Form** patterns (react-hook-form + primitives) over ad-hoc deprecated `FormEvent` flows unless there is a specific reason not to. For shadcn **Select**, default `SelectContent` to **`position="popper"`** (below the trigger with a small gap)—not **`item-aligned`**, which overlaps the selected value. **Tooltip** primitives use semantic popover tokens (`bg-popover`, `text-popover-foreground`)—not inverted `bg-foreground`/`text-background`; shortcut **`Kbd`** inside tooltips uses `bg-foreground/10` and `text-muted-foreground`. Prefer the **inset** sidebar shell (`variant="inset"`) with minimal borders and glass/gradient surfaces over heavy bordered layouts; preserve the **emerald brand** palette unless directed otherwise. For sidebar theming, keep clear sidebar-vs-content contrast: in **dark mode** the sidebar should be noticeably **lighter** than the main content; in light mode slightly **darker** (still clearly light mode); use a subtle emerald-tinted neutral—neither flat gray nor saturated “vomit” green. Omit redundant **`SidebarGroupLabel`** section headings on nav groups (e.g. “Navigation”) unless explicitly requested. **Sonner error toasts** (`components/ui/sonner.tsx`) use a **solid opaque background**—not translucent glass. Clerk **`SignInButton mode="modal"`** should open **viewport-centered** for signed-out users via shared `clerkAppearance` on `ClerkProvider` (`lib/clerk-appearance.ts`). Sidebar **`ThemeToggle`** shows **Sun** when light mode is active and **Moon** when dark mode is active (state icons, not “switch to” icons). **Quiet Terminal** is the app-wide dark-first design language: charcoal canvas, **single mint accent** (`--primary`/`--up`) plus **coral** (`--down`), **Fraunces** display headings (`--font-heading`, italic accent words) with monospace numerics, white-alpha hairline borders, and ambient grid/glow. In this language use **Lucide icons only (no emojis)**, route every color through **semantic design tokens** (never raw `text-white`/`black` or `bg-white`/`black`), avoid purple/blue accents, and render sparklines/mini-charts as **lightweight inline SVG** (not a chart library). Keep **light mode functional** with fitting colors. Apply cohesive control radius via shadcn primitives in `components/ui/`—**`rounded-md`** on Button, Input, Select trigger, Dialog, Card, and related shells—not default shadcn pill radii (`rounded-4xl`) and not one-off per-page overrides. Clickable shadcn controls (`Button`, `Toggle`, `Switch`, `Label`, `TabsTrigger`, sidebar menu buttons, `Select`, `DropdownMenu`) should use **`cursor-pointer`** on hover—add it in the **primitive** base classes under `components/ui/`, **not** via a global `button { cursor: pointer }` rule in `app/globals.css`; raw native `<button>` elements outside primitives keep explicit `cursor-pointer` at their call sites.
- When using reference screenshots for UI, treat them as layout/hierarchy guidance unless the user asks for a pixel-perfect copy; preserve the app's existing color themes and typography when the user clarifies that the reference is not literal.
- For catalyst research, avoid hardcoded ticker/company-specific event maps, deterministic bucket/checklist search plans, and ticker-specific discovery vocabulary; use open-ended hosted search breadth and general discovery patterns that surface company-specific strategic milestones from current news—not only official calendar events—for any proper company. Prefer **generalized** pipeline fixes (shared scoring, prompts, dedupe passes) over solutions that overfit one ticker or product. For catalyst **quality guardrails**, prefer **loose model-led prompt guidance** (`formatResearchCatalystThreadCoherenceBlock`, extraction self-checks) over rigid deterministic post-extraction filters—avoid hard title-token overlap gates, blanket ticker-results horizon filters, strict `supportsFields` enforcement, and grace-window drops that would block legitimate ongoing milestones; preserve model discretion for messy strategic catalysts. Do **not** add per-run event archives or explicit lifecycle tables for cross-run retention—prefer the storage-neutral hybrid reconcile path (load prior events in-memory, match/carry-forward, still delete+insert upsert) to limit Convex free-tier storage.
- When debugging missing catalyst results or empty tool output or “silent” model behavior, do not attribute outcomes to cache without evidence; prioritize retrieval, extraction, and provider/AI-SDK paths first; use Convex function logs or a local script that mirrors the same `generateText` call before speculative fixes — Convex **actions** do not run on the developer’s machine, so `localhost` ingest or similar will not see those requests.
- For **GPT-hosted web research**, keep **GPT as the orchestrating model** and use **Exa’s `webSearch` tool** (`@exalabs/ai-sdk`) with the Vercel AI SDK (`generateText`, `tools.webSearch`, `stopWhen`) instead of OpenAI’s built-in web search unless native OpenAI search is explicitly required; set **`EXA_API_KEY`** where Exa runs (e.g. Convex actions). Exa’s `webSearch` is **model-agnostic**—it works with any tool-calling model plus `stopWhen: stepCountIs(n>1)`, so the orchestrator need not be GPT/OpenAI.
- For catalyst events, keep **`summary`** and **`whyItMatters`** concise in extraction prompts and structured-output guidance (roughly one to two short sentences for `summary`, one short sentence for `whyItMatters` unless strictly necessary).
- In catalyst results UI, prefer a concise **table** (`Table` / related primitives) instead of sprawling card-heavy layouts unless a screen truly needs cards; on the **ticker route** (`/${TICKER}`), follow **Quiet Terminal** patterns—Fraunces headline with **mono ticker symbol inheriting heading foreground** (not a mint accent), pulsing-dot status pill, **3-bar impact meter** (mint / neutral / muted), **hairline outer frame** on the results table (`rounded-xl border border-border/40`, optional `bg-card/30`—not borderless, not heavy glass), hairline row dividers with **mono uppercase** column labels, **mint-bordered quote** under significance for AI takeaway, and **mint source links** with external-link hover; ticker **Mark stale** / **Copy data** / **Save to portfolio** buttons use **`rounded-md`** to match sidebar controls—not default shadcn **`Button`** **`rounded-4xl`** pills. **Copy data** sits between Mark stale and Save; it copies the **filter-aware** visible table rows to clipboard as CSV via `lib/catalyst-events-csv.ts` (respects active Expected impact filters). Include an **Expected impact** column (icon + label from `expectedImpact`, default `size-3.5` icons in `components/research/catalyst-events-table.tsx`) **before Sources**; optional impact filter toggles reuse those **same icons**, keep the label **Expected impact** (never shorten to "Impact"), and make the **active filter state** visually obvious; show **one clickable link per source** when an event has multiple sources rather than collapsing into a single “N links” control; in the **Strategic significance** column, render **`summary`** in default body text and **`whyItMatters`** in **muted** foreground; **sort Timing unknown events with no quarter first** (`sortCatalystEventsByAnchor` in `lib/research-results-utils.ts`), before dated/quartered rows; keep **research loading / blocker states on the ticker route** visually aligned with the home hero (narrow `max-w-5xl` column, viewport-centered shell via `lib/research-route-layout.ts`); use a **spinner only** for in-progress research—**no skeleton** placeholders. **Mark stale** uses **`RefreshCw`** idle / **`Loader2`** loading; **Save to portfolio** uses **`Briefcase`** idle / **`Loader2`** loading (`size-4`, `aria-hidden`).
- Saving catalyst research from results to the portfolio should be **all-or-nothing** for that run — do **not** ask users to pick individual catalyst rows. On the ticker save flow, the portfolio name field is editable only when Save target is **Create …**; disable it when saving to an existing portfolio. **Portfolio names must be unique per signed-in user** (enforced in `convex/portfolios.ts` via `by_user_and_name`). Surface **all** user-facing Convex failures (portfolio saves, research quotas, concurrent-run limits, etc.) via **`getConvexMutationUserMessage` / `showConvexMutationErrorToast`** (`lib/convex-mutation-error.ts`)—not generic **Server Error** text from action wrappers. Do **not** auto-open the create-portfolio dialog on **`/portfolios`** when the user has zero portfolios—open only via explicit user action.
- On first load or full refresh of the home page, focus the ticker search **`Input`** (e.g. `autoFocus` **after** `{...field}` from react-hook-form so the spread does not override it). After clicking a suggested/popular ticker on **`/`**, refocus the search input so the user can press **Enter** to start research immediately. Keep the home hero **subheading** succinct and **complementary** to the pill tagline (`AI-powered stock catalyst research`)—outcome-focused copy (e.g. catalyst types over 12 months), not a second AI-research tagline.
- In catalyst **extraction**, when sources describe the **same dated or named real-world occasion** (e.g. a flagship conference and expected announcements there, or the same production start under generic vs site-specific headlines), output **one merged event** with product/launch detail in `summary` / `whyItMatters`—no separate rows that differ only by angle or `eventType`. **`lib/research-occasion-match.ts`** scores same-occasion pairs for in-run dedupe and cross-run reconcile; a post-reconcile dedupe pass catches new + carried-forward duplicates. But for **lock-up, float, insider-supply, and similar corporate milestones**, do **not** stitch **large share counts**, **percentages**, and **calendar anchors** from **different** snippets into one event unless **one** cited source states them together; conflicting dates across sources should yield **separate events** or a single lower-confidence row, not a hybrid. For **quarter-named publication/disclosure catalysts** (e.g. quarterly vehicle production/delivery reports), anchor timing to the **expected release month** (`timingShape: period`, `periodKey: YYYY-MM`)—put the covered fiscal quarter in the title/summary, not in `periodKey` (e.g. Q2 2026 deliveries expected early July → `2026-07`, not `2026-Q2`). Use `period` + `YYYY-Qn` only when activity **during** that quarter is the catalyst. Do **not** combine a **title** from one headline with **summary**, **timing**, or **sources** from unrelated sections of the same multi-topic roundup URL—one catalyst thread per row; split or drop incoherent merges rather than cross-wiring governance, deliveries, and regulatory angles.
- For catalyst **extraction**, include the **canonical issuer name** (e.g. from Finnhub) in the prompt when available—especially for **short or ambiguous tickers**—so lock-up, float, and insider-supply rows are not conflated with the wrong company’s facts.

## Learned Workspace Facts
- Use **Bun** for installs, scripts, and CLI tools (`bun install`, `bun run …`, `bunx …`); **never** `npm` or `npx`. `bun.lock` is the only committed lockfile—do not add `package-lock.json`. Convex: **`bunx convex dev`** during development; **`bunx convex deploy`** for production backend only (Vercel does not deploy Convex).
- Stonkseer: users research upcoming catalysts (next 12 months) for a ticker and can save to a portfolio. The app uses Next.js and React with shadcn UI, Convex for backend and reactivity, Clerk Google SSO, and the Vercel AI SDK for structured research. Starting research from **`/`** navigates to **`/${normalizedTicker}`** (`app/(main)/[ticker]/`); **`sessionStorage`** key **`stonkseer:activeResearch:{SYMBOL}`** bridges **`{ runId, anonymousTokenHash? }`** so the ticker page can subscribe to Convex while anonymous auth stays off the URL. The ticker-page heading shows the **Finnhub company name** alongside the symbol via `formatIssuerHeading` in `lib/ticker-display.ts` (e.g. “Apple Inc. (AAPL)”). **`/portfolios`** requires sign-in; signed-in users pick among multiple portfolios via a dropdown. Ticker research results via **`getRunResults`** return all stored canonical events—**no** `filterUpcomingCatalystEvents`—unlike portfolio upcoming views.
- Global client providers (Convex+Clerk bridge, theme, auth sync, sidebar tooltip provider, and similar) live under `components/providers/`; wire the tree in `components/providers/app-providers.tsx` and keep `app/layout.tsx` thin. The main app shell/sidebar live in `components/main-shell.tsx` and `components/app-sidebar.tsx`, with route pages under `app/(main)/`. The shell uses **`Sidebar variant="inset"`**; avoid stacking **`min-h-svh`** on nested inset layers (`SidebarInset` already has desktop margins)—use **`min-h-0 flex-1`** on shell wrappers and page **`md:min-h-full`** so desktop pages do not scroll when content fits. The **document never scrolls**: `MainShell` uses `SidebarProvider className="h-svh min-h-0 overflow-hidden"` and scrolls content internally via **`MainScrollPane`**, with `html, body { overscroll-behavior-y: none }` in `app/globals.css` to kill rubber-band overscroll—when touching shell scrolling, verify overflowing pages (e.g. research results tables) still scroll. **`TickerTape`** in `MainShell` shows on **all main-shell pages**; it reads **`api.marketTape.getSnapshot`** (Finnhub refresh via `marketTapeActions:refreshMarketTape` + 15-min cron, **only during US extended hours**—4:00 AM–8:00 PM Eastern weekdays per `lib/us-market-hours.ts`; outside session the last snapshot is kept). Symbols live in **`lib/market-tape-config.ts`**—US ETF proxies (SPY, QQQ, DIA, VIXY, IBIT, ETHA) plus tickers; Finnhub free tier does not support index (`^…`) or Binance crypto symbols. **Tape display labels are the quote symbol** (e.g. `QQQ`, `IBIT`). Sidebar nav items are **Search** and **Portfolios** only (Calendar UI placeholder removed; `/calendar` falls through to `[ticker]` as `CALENDAR`—Finnhub earnings-calendar in research is unrelated). Mobile shell behaviors: the mobile-only top bar uses **`bg-background/80`** (same surface as **`TickerTape`**, not `glass`) and shows the current section title from `getMobileHeaderTitle(pathname)` (`lib/app-navigation`) next to **`SidebarTrigger`** (`max-md:size-11` / 44px touch target on mobile; desktop stays `icon-sm`); nav-link, **Sign In**, and **Sign Out** taps close the mobile sidebar (`setOpenMobile(false)`) before navigation or the Clerk modal; the collapsed desktop sidebar swaps the logo for the trigger icon on hover (no overlay).
- The MVP intentionally allows one anonymous ticker research trial before sign-in; saving to a portfolio requires Google sign-in. User-initiated research reuses a prior completed run for the same ticker for **7 days** when strategy version and event data match (`RESEARCH_CACHE_TTL_MS` in `convex/research.ts`); cache hits do not count toward quotas. Admins (`yokeyeong@me.com`, `role: "admin"`) **bypass daily/concurrent research quotas** and can **Mark stale** on completed ticker results (`api.research.markSymbolResearchStale`), setting `stocks.researchCacheInvalidatedAt` so the next search bypasses cache; non-admins never see the control (`lib/admin.ts`). Signed-in users are capped at **10** non-cache research runs per rolling **24 hours** and at most **2** concurrent in-progress runs (`convex/research.ts`).
- Anonymous research protection uses layered controls: deterministic ticker normalization/validation, hashed IP bucket, signed browser token, cache reuse, and invalid tickers must not consume the anonymous trial; anonymous runs use the **same hosted-search query breadth** as signed-in runs (no separate anonymous web-query budget), while **daily anonymous non-cache quotas** still apply (per IP and signed-browser-token gates in `convex/research.ts`).
- The anonymous research endpoint is a Next.js Route Handler because it needs request headers, IP-derived signals, and HTTP-only cookie handling; authenticated portfolio saves should use Convex mutations.
- Portfolio holdings auto-refresh on a **30-day per-holding clock** (`lastPortfolioRefreshAt` on `portfolioStocks`, `PORTFOLIO_REFRESH_INTERVAL_MS` in `lib/portfolio-refresh.ts`): a daily Convex cron at 08:00 UTC queues `source: "refresh"` runs for stale active holdings, independent of ad-hoc research and the 7-day research cache; successful refresh runs update canonical stock catalysts and bump `lastPortfolioRefreshAt`. Each ticker has **one canonical `catalystEvents` set per `stockId`**, replaced on every completed research run after in-action cross-run reconciliation merges carry-forward rows from the prior set (`upsertResearchResults` in `convex/researchInternal.ts` still delete+inserts); portfolios hold symbols only and read live stock events via `loadCatalystEventsWithSources`—no `trackedEvents` snapshot layer or per-run event archive.
- Research runs in Convex actions: validate tickers deterministically using Finnhub market data when `FINNHUB_API_KEY` is set; **`warnExpectedEnvMissing`** logs **`console.warn` once per warm isolate** when expected research env vars are absent (`FINNHUB_API_KEY`, `AI_GATEWAY_API_KEY`/`VERCEL_OIDC_TOKEN`, `EXA_API_KEY`, enabled provider model ids)—not invalid-key checks. **AI Gateway observability:** every Gateway-routed `generateText` in [`lib/research-gateway-observability.ts`](lib/research-gateway-observability.ts) tags calls with `run`, `ticker`, `leg`, `source`, `env`, and `strategy`; signed-in runs also set Gateway `user` to the Convex user id—filter in Vercel project → **AI Gateway**. Set `STONKSEER_GATEWAY_ENV` on the **Convex** deployment (`production` on prod); optional `AI_GATEWAY_EXTRACTION_FALLBACK_MODELS` enables merge/extraction model failover. Use a stable `AI_GATEWAY_API_KEY` in Convex (not OIDC alone). Finnhub profile/industry is **company context** in the merge prompt, while Finnhub profile/earnings/**earnings-calendar** snippets remain in the **evidence pool** (`finnhub_metadata` provenance). **All** enabled hosted search providers in `CATALYST_HOSTED_SEARCH_PROVIDERS` run **in parallel** each run (**Gemini** `google.tools.googleSearch`; **OpenAI** GPT orchestrator + Exa `webSearch`; **Anthropic** `webSearch_20250305`; **xAI** `x_search`). Each provider produces a **full catalyst research report** (`buildCatalystReportPrompt`—chat-quality synthesis with timing, status, why-it-matters, and source URLs; no bucket checklists or company-specific vocabulary) plus **tool-excerpt-only snippets** and a `seenUrls` list; the old digest round-robin quote fallback (`digest_fallback`) is **deleted**—never attribute model digest text to URLs. After round 1, a **dynamic follow-up pass** ([`lib/research-followup.ts`](lib/research-followup.ts)) has a model (env `CATALYST_FOLLOWUP_MODEL`, falls back to `AI_GATEWAY_MODEL`) write up to **6** targeted queries (`CATALYST_FOLLOWUP_MAX_QUERIES`) from the merged reports—never hardcoded—run via the **Exa search API** (`EXA_API_KEY`, provenance `follow_up_search`); **neither `AI_GATEWAY_MODEL` nor the follow-up query writer use tools or built-in web search**—retrieval stays on hosted-search providers and Exa. Web evidence caps: **64** snippets (`MAX_WEB_SNIPPETS`) at **2500** chars per quote (`WEB_SNIPPET_QUOTE_LENGTH`, including Exa `maxCharacters`), after URL dedupe and domain diversification. Optional **Exa Contents** deep-read on up to **8** URLs ranked by provider hits + excerpt length ([`lib/research-exa.ts`](lib/research-exa.ts)). The **merge/extraction** step via Vercel AI Gateway (`AI_GATEWAY_MODEL`) receives the **provider reports plus evidence snippets** (keyword `candidates` hints are removed) and uses **`formatResearchBreadthExtractionBlock`** plus **`formatResearchTimingExtractionBlock`** (`lib/research-themes.ts`) for cross-family coverage and publication-release timing; merge/extraction **authors fresh structured JSON** (it can reconcile timing from titles/snippets even when a hosted-search report said timing unclear). Extraction returns up to **`MAX_CATALYST_EVENTS` (50)** events (`lib/research-contract.ts`). **`formatResearchProceedingMergeBlock`** ([`lib/research-proceeding-merge.ts`](lib/research-proceeding-merge.ts)) guides regulatory/legal proceeding merge in extraction and hosted-search reports. **Within-run dedupe** ([`lib/research-inrun-dedupe.ts`](lib/research-inrun-dedupe.ts), shared scoring in [`lib/research-occasion-match.ts`](lib/research-occasion-match.ts); env `CATALYST_INRUN_DEDUPE_ENABLED`, default on) runs after extraction and again after cross-run reconcile. **Catalyst timing** uses `timingShape` in extraction and [`lib/catalyst-timing.ts`](lib/catalyst-timing.ts) for display—`formatTimingFragment` renders month anchors as full names (e.g. **April 2026**, not `2026-04`): never invent a **12-month-horizon** `windowEnd` or anchor `windowStart` to the run date for open-ended/ongoing milestones—use **`open`** with source-backed past starts and show **Since … (ongoing)**, not **From {today} (ongoing)**. After grounding and again after final dedupe, **`repairCatalystEventTiming`** in [`lib/catalyst-timing-infer.ts`](lib/catalyst-timing-infer.ts) regex-repairs missing/broken structured timing from title/summary/source quotes when `eventTimingLabel` would be **Timing unknown** (caps confidence, downgrades `confirmed` → `likely`); **`upgradeContradictoryTimingShape`** in `catalyst-timing.ts` upgrades orphan anchors before coercion. The **Timing** column reads structured fields only—not summary prose; the **Quarter** column is a render-time grouping from the sort anchor (`parseSortAnchor` / `quarterKeyFromDate` in `catalyst-events-table.tsx`), not a stored field. Then **provenance-aware grounding** ([`lib/research-grounding.ts`](lib/research-grounding.ts)): snap source quotes to snippet excerpts by URL (`evidence_snippet`); keep sources whose URL any provider saw but lacks an excerpt as **`report_derived`** (model quote kept); drop only sources/events citing URLs nobody saw. Then **hybrid cross-run reconciliation** ([`lib/research-reconcile.ts`](lib/research-reconcile.ts)): load prior canonical events in-memory (`getPriorCanonicalEvents`); deterministic old/new match (URL overlap, `periodKey`+`eventType`, title similarity); policy-filter expired/confirmed-only rows; auto-keep speculative/likely prior-only events with X sources; AI review (`reconcile-carryforward` Gateway leg) for ambiguous carry-forward candidates; seed up to **4** prior themes into follow-up Exa queries; preserve `createdAt`/`lastVerifiedAt` on carried rows (`prior_run_carryforward` provenance)—still **delete+insert** upsert, no archive tables. Finnhub earnings-calendar rows merge in as baseline earnings events when extraction omits them. Default hosted search steps/uses: **8** (`CATALYST_OPENAI_EXA_AGENT_MAX_STEPS`, `CATALYST_ANTHROPIC_WEB_SEARCH_MAX_USES`); **~3–4 GPT Gateway calls per OpenAI+Exa pass is normal** multi-step agent behavior—Exa tool/API hits meter separately from Gateway. **`No object generated: response did not match schema.`** means merge/extraction structured output failed Zod validation—the run failed before grounding; it is **not** the post-grounding empty path (`No cited catalyst events found from the current web/news sources.`); invalid `datePrecision` literals are coerced via `normalizeDatePrecision()` in `lib/research-contract.ts` before validation. **Anthropic** hosted search is typically the costliest parallel leg (~**$0.50**/run on Sonnet 4.6: per-search fees plus accumulated search context in one call); **`claude-haiku-4.5`** supports the same `webSearch_20250305` tool via `CATALYST_ANTHROPIC_SEARCH_MODEL` even when AI Gateway model cards omit web search. Strategy version [`RESEARCH_STRATEGY_VERSION`](lib/research-strategy.ts) busts cache on pipeline changes (now `catalyst-timing-repair-v1`). For Anthropic hosted search, use `anthropic.tools.webSearch_20250305`; `webSearch_20260209` produced an unsupported `anthropic-beta` header in this Gateway path.
- The GitHub repository is `https://github.com/0xGuvnor/stonkseer`, with `master` tracking `origin/master`; the Vercel project is `0xguvnors-projects/stonkseer` at `https://stonkseer.vercel.app`. On Vercel, `NEXT_PUBLIC_*` values (including `NEXT_PUBLIC_CONVEX_URL`) are inlined at build time, so they must be set in the project's environment variables for each environment that production or preview builds use. Env templates are split: **`.env.example`** for Next.js/Vercel vars and **`.env.convex.example`** for Convex deployment vars.
- The Convex dashboard project is named `stonkseer`, and the local CLI has been connected to it; **pair Clerk and Convex per environment**—local/Cursor uses dev Clerk keys (`pk_test_`/`sk_test_`) with dev `NEXT_PUBLIC_CONVEX_URL` and dev Convex `CLERK_JWT_ISSUER_DOMAIN`; Vercel Production uses live Clerk with prod Convex URL and prod issuer (mixing dev/prod across surfaces breaks JWT validation in `convex/auth.config.ts`); default **Vercel Preview** to dev Clerk + dev Convex. Clerk frontend/server keys belong in Next.js/Vercel env; the Clerk JWT issuer belongs in each Convex deployment’s env vars; create the Clerk **“convex” JWT template** in both Clerk Development and Production. Ship Convex prod separately with `bunx convex deploy`—Vercel does not deploy the backend. If real Clerk keys are set, keyless `.clerk/` fallback is not required and generated `.clerk/` content stays out of git. Clerk **production** requires a **domain you own** with DNS records—not a `*.vercel.app` URL (per Clerk deployment docs).
- Research runs take **~8–10+ minutes**, so the app offers **opt-in browser notifications** when a run finishes: entirely client-side via the **Notification API** plus a `localStorage` watchlist (keys `stonkseer:researchNotifyWatchlist`, default flag `stonkseer:researchNotifyDefault`, change event `stonkseer:research-notify-watchlist-changed`); helpers live in `lib/research-completion-notifications.ts`, the watcher provider in `components/providers/research-completion-notifier.tsx`, the opt-in switch in `components/research/research-notify-toggle.tsx`, with **sonner** toasts (`components/ui/sonner.tsx`) as in-page fallback.
- xAI **Grok X** (`x_search`) via **Vercel AI Gateway** (`CATALYST_XAI_SEARCH_MODEL` as a gateway model id): the SDK may return **`tool-call`** and **`source`** URL parts without normalized **`tool-result`** rows, so `result.toolResults` can be empty even when search ran; derive X/Twitter snippets from parsed **`x_search` post text** only—do **not** attribute the model digest to X URLs. With **Vercel AI SDK 6**, top-level **`result.toolResults` reflects the last step only**—merge across **`result.steps`** when collecting posts, and if **no** `x_search` tool results appear in steps, **also** scan top-level **`result.toolResults`** for Gateway edge cases. Disabling **`enableImageUnderstanding` / `enableVideoUnderstanding`** saves cost and latency but can **reduce relevance** on posts that lean on images or video; env-gate or turn media back on if X evidence quality drops. Direct `xai.responses()` is a different auth path (`XAI_API_KEY`); do not assume it when the product standard is Gateway-only. `x_search` searches **X/Twitter only** (keyword, semantic, user, and thread modes—Grok picks; users never type queries) and is agentic server-side via the Responses API `max_turns`; the AI SDK `xSearch()` exposes only `allowedXHandles`/`excludedXHandles`/`fromDate`/`toDate`/`enableImage…`—there is **no client `maxUses`/step budget** like Exa or Anthropic.
